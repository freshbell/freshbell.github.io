/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains:[
            'www.notion.so',
            'image.unsplash.com',
            ''
        ]
    }, 
    basePath: "/"
}

module.exports = nextConfig
